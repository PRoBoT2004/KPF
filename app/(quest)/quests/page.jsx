import QuestHall from "@/components/pixel/QuestHall";

export const metadata = {
  title: "Hall of Quests — All Projects",
  description:
    "Every project — flagship SaaS (Shop-ERP, Touchpe, Vitran), client work (Enda, Launchpad, OnGo, Biocon, Color Pencil) and lab builds. Walk the gallery.",
};

export default function Page() {
  return <QuestHall />;
}
