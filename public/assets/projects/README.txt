Drop project images / mockups here and they become the main card automatically.

Expected filenames (PNG or JPG — keep the exact name):
  shop-erp.png    → Shop-ERP card
  touchpe.png     → Touchpe card
  vitran.png      → Vitran card

Tips for the best look:
- A clean product screenshot or a device/browser mockup works great.
- Roughly 16:9 / 4:3 landscape; the card crops to fit.
- Until a file exists here, a styled "product window" mockup shows instead
  (no broken images), so the site always looks finished.

To add more (or use a different name), tell me and I'll wire the path in
src/data/portfolio.js (the `image` field on each project).
